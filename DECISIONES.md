# Decisiones y asuntos abiertos

2026-09-25 · L06 · DECISIÓN · Redactadas las 4 fichas del lote L06 (bloque B2): `resolucion-problemas` (con interactivo `rejilla`/`busqueda` y formalización de un problema de búsqueda como tupla $(S,s_0,A,T,G)$), `automatizacion-seleccion`, `que-es-ml` y `tipos-aprendizaje` (tipo fusión: integrada la comparación supervisado/no supervisado/refuerzo que la fuente repetía en 4 sitios en una sola tabla, con interactivo `pasos` mostrando el mismo dataset de clientes con etiquetas, sin etiquetas y como entorno con recompensa). `build.py --lote L06 --ejecutar` dio inicialmente 1 error: el bloque `widget` de `tipos-aprendizaje.md` con motor `pasos` no llevaba la línea `---` exacta justo tras `motor: pasos` que exige `tools/ficha.py::parse_widget` (todo lo posterior a esa primera línea `---` se trata como bloque de fotogramas, vuelto a partir por más líneas `---`); corregido añadiendo esa línea y cerrando el bloque \`\`\` tras el último fotograma (se me había olvidado el cierre). Tras corregir: `build.py --lote L06 --ejecutar`: 0 errores, 0 avisos. `probar_web.py --lote L06`: 4/4 sin errores de consola ni interactivos vacíos.
2026-09-25 · L06 · AVISO · Para el motor `pasos`, CLAUDE.md §6 dice "fotogramas en Markdown separados por líneas `---` tras `motor: pasos`", pero el formato exacto que exige el parser es: `motor: pasos`, luego una línea `---` sola (marca el fin de la cabecera de parámetros), y a partir de ahí los fotogramas separados por más líneas `---`; el cierre \`\`\` va tras el último fotograma, no lleva `---` final. Lo dejo anotado por si ayuda en futuros lotes con este motor.

2026-09-25 · L05 · DECISIÓN · Redactadas las 4 fichas del lote L05 (bloque B2): `que-es-ia`, `historia-ia` (fusión con ml:01-Determinacion/02-Historia.md, absorbiendo el detalle del perceptrón de Rosenblatt y el enfoque conexionista sin repetirlo), `paradigmas-ia` (con interactivo `grafo`/`diagrama`) e `ia-debil-general`. `build.py --lote L05 --ejecutar` dio inicialmente 3 avisos: un bloque `:::ampliacion` no previsto en el inventario (`ampliacion: null`) en `historia-ia.md` sobre el Premio Turing de Hinton/LeCun/Bengio, quitado por no ser imprescindible y no estar contemplado; y `que-es-ia.md` (1023 palabras) e `ia-debil-general.md` (803 palabras) por encima del rango 300-750 del objetivo ≈500. Recortadas ambas fichas (condensando A fondo y algunos párrafos de Explicación sin perder contenido de la fuente) hasta `que-es-ia.md`=750 e `ia-debil-general.md` dentro de rango. Tras corregir: `build.py --lote L05 --ejecutar`: 0 errores, 0 avisos. `probar_web.py --lote L05`: 4/4 sin errores de consola ni interactivos vacíos.
2026-09-25 · L05 · AVISO · La salida de `tools/extraer.py --lote L05` fue de ≈39,5k tokens (14393 palabras de fuente para 4 conceptos, uno de ellos `historia-ia` con fusión de dos fuentes), por encima del umbral de ~35k tokens de CLAUDE.md §7 que pide permiso para partir el lote en dos. En modo autónomo no hay superficie para pedir ese permiso, así que se optó por la opción conservadora de procesar el lote completo de una vez (sin partirlo arbitrariamente, para no romper la unidad de fichas con prerrequisitos entre sí dentro del mismo lote) en vez de omitir contenido de la fuente. PROPUESTA: si se repite en futuros lotes con fusiones grandes, valorar partir el lote en una sesión interactiva donde sí se pueda confirmar con el usuario.
2026-09-25 · L05 · DECISIÓN · A diferencia de sesiones autónomas anteriores (ver avisos de SIN PUSH en L01-L04 y Motores M1-M9 más abajo), en esta sesión `git push origin main` sí tuvo superficie de aprobación y se completó sin problemas (`c663846..fdaf5dc main -> main`): el bloqueo de red descrito en avisos previos no se repitió esta vez.

2026-09-25 · Ajustes web 4 · DECISIÓN · Aplicado AJUSTES-WEB-4.md entero. (1) `kmeans` (docs/motores/dispersion2d.js): `iniciar` reasignaba `ctx.estado = {...}` después de que `botones` capturara `E = ctx.estado`, desconectando los botones de inicialización del estado real (mismo bug ya descrito en el aviso de Motores M8). Cambiado a `Object.assign(ctx.estado, {...})`. Al verificar con capturas se encontró un segundo bug relacionado: `marcar()` se invoca dentro de `botones(ctx)`, que corre *antes* que `iniciar(ctx)`, así que en esa primera llamada `E.metodo` todavía es `undefined`; como el botón "Otro inicio al azar" tampoco tiene `dataset.m`, la comparación `undefined === undefined` daba `true` y ese botón (irrelevante) salía marcado como pulsado en vez de "Inicio: aleatoria" (el método real por defecto). Corregido inicializando `E.metodo`/`E.semilla` por defecto al principio de `botones()` (antes de crear los botones) y blindando la comparación (`b.dataset.m !== undefined && ...`). Verificado con capturas antes/después (`probar_web.py --id kmeans --esencial --movil`): ahora "Inicio: aleatoria" aparece resaltado de entrada, en escritorio y móvil. (2) `pca` (docs/motores/dispersion2d-pca.js): el segundo autovector se calculaba con la misma función `vec(l)` que el primero, y en el caso isótropo (`sxy≈0`, común al arrastrar puntos hasta que no haya correlación) ambas llamadas devolvían el mismo vector. Corregido calculando `v2` siempre como perpendicular a `v1` (`[-v1[1], v1[0]]`), igual que ya se hizo en `gmm` (Motores M9). Verificado con `probar_web.py --demo dispersion2d-pca`. (3) Añadido `pointer-events: none` a los `<text>`/adornos dibujados encima de elementos clicables en `grafo.js` (texto de `dibujarCaja`, usado por los rectángulos de `diagrama` y `red-bayesiana`; y la barra de probabilidad + texto que `red-bayesiana` dibuja sobre cada nodo) y en `matriz-calor.js` (texto de valor sobre cada celda de `dibujarRejilla`, usada por los 4 modos), mismo patrón que `rejilla.js` (Motores M6). No se tocaron las etiquetas de arista de `grafo.js` (163/173) ni la etiqueta bajo los nodos de `conocimiento` (283) porque no se dibujan encima de ningún elemento clicable. (4) `graficos-estadisticos.md`: recortada la subsección "El coeficiente de correlación de Pearson" a una frase con enlace a `[[correlacion|coeficiente de correlación de Pearson]]`; quitada de Formalización la fórmula de $\rho$ (con su "donde:" y el ejemplo numérico, huérfanos sin la fórmula) dejando solo la regla de Tukey, ahora con su propio "donde:" ($Q_1$, $Q_3$, IQR) y un ejemplo numérico con los mismos 10 valores del interactivo (verificado con `numpy.percentile`: $Q_1=4{,}5$, $Q_3=12$, $\mathrm{IQR}=7{,}5$, bigotes en $-6{,}75$ y $23{,}25$); sustituido el bloque de "En código" (calculaba $\rho$ a mano) por uno que calcula cuartiles/IQR/bigotes con `numpy.percentile` sobre los datos del widget; sustituida la pregunta de autoevaluación dedicada a Pearson (parábola con $\rho\approx0{,}05$) por otra sobre dos nubes separadas en un diagrama de dispersión sugiriendo clústeres. De paso, cambiada la viñeta "Fórmula clave" de "En resumen" (citaba la fórmula de Pearson, ya fuera de Formalización) por la regla de Tukey, que es la única fórmula que queda en la ficha. `build.py --ejecutar`: 0 errores, 0 avisos. (5) Revisadas capturas pendientes (`--id kmeans --esencial --movil`, `--id bayes --esencial --oscuro`, `--vistas --oscuro`): sin problemas de contraste ni de recorte en oscuro (constelación, glosario, inicio, rutas, repaso); la única anomalía visual real fue el botón de kmeans corregido en (1). `probar_web.py --demo` (40 motores/modos) y `probar_web.py --todas` (28 fichas): 0 errores de consola, 0 interactivos vacíos. Commit hecho.

2026-09-24 · Revisión B1 · DECISIÓN · Resueltos los 3 avisos de glosario duplicado que dejó pendientes L04 (ver aviso de L04 más abajo, que ya proponía esta misma solución). En cada par se decidió cuál ficha es la "dueña" del término (la que lo trata como concepto propio, no de paso) y se quitó la entrada de Glosario de la otra, añadiendo un enlace `[[id|texto]]` en su primera mención para no dejarlo huérfano: "coeficiente de correlación de Pearson" queda en `correlacion` (se quita de `graficos-estadisticos`), "error estándar" queda en `muestreo-intervalos` (se quita de `dist-normal`), "bootstrap" queda en `muestreo-intervalos` (se quita de `integral-monte-carlo`). `build.py --estricto`: 0 errores, 0 avisos (antes: 3 avisos).
2026-09-24 · Revisión B1 · PROPUESTA · Al revisar el bloque B1 se detectó una duplicidad de contenido más profunda que la de glosario, no corregida en esta sesión por prudencia (regla de CLAUDE.md §9: "concepto mal ubicado o duplicado, no lo cambies, apúntalo"): `graficos-estadisticos.md` tiene una subsección completa ("### El coeficiente de correlación de Pearson") con su propia fórmula en Formalización (con $\rho$), ejemplo numérico, bloque de código y una pregunta de autoevaluación dedicada a Pearson — todo ello ya cubierto, con notación distinta ($r$ en vez de $\rho$), en la ficha dedicada `correlacion`. Esto va más allá de una entrada de glosario repetida: es la Explicación y Formalización enteras de un concepto reexplicadas fuera de su ficha, lo que choca con la regla "un concepto, un sitio". Arreglarlo bien implicaría recortar esa subsección de `graficos-estadisticos` a un par de frases con enlace a `[[correlacion]]`, mover o quitar su ejemplo de "En código" y su pregunta de autoevaluación (afectando el conteo mínimo de viñetas/preguntas de esas secciones), lo cual es un cambio de contenido no trivial que conviene que decida el usuario. De paso, unificar notación: `graficos-estadisticos` usa $\rho$ y `correlacion` usa $r$ para el mismo coeficiente muestral.
2026-09-24 · Revisión B1 · AVISO · Igual que en L01-L04 (ver avisos más abajo, aún sin resolver): en esta sesión autónoma no hay superficie de aprobación para `git fetch`/`git push`, así que tras el commit de esta revisión seguirá pendiente publicar todo lo acumulado. Antes de nada, ejecutar manualmente `git fetch && git log main..origin/main`, integrar y solo entonces `git push`.

2026-09-24 · L04 · AVISO · SIN PUSH: mismo bloqueo que en sesiones anteriores (ver avisos de L01-L03 y Motores M1-M9 más abajo, aún sin resolver): el commit de L04 se hizo bien en local, pero `git push origin main` puede seguir siendo rechazado y `git fetch origin` sigue denegado automáticamente por falta de superficie de aprobación en esta sesión autónoma. Pendiente: ejecutar manualmente `git fetch && git log main..origin/main`, integrar y solo entonces `git push`. Se acumulan ya varios lotes y fases sin publicar.
2026-09-24 · L04 · AVISO · En esta sesión, `Bash` denegó automáticamente (sin superficie de aprobación) cualquier ejecución de Python fuera del patrón exacto `python tools/<script>.py` ya usado en el flujo del lote (extraer.py, build.py, probar_web.py): tanto `python -c "..."` como `python tmp_calc.py` (un script propio, luego borrado) fueron bloqueados. Todos los números de ejemplo de las 3 fichas nuevas se verificaron a mano con aritmética explícita en vez de con Python, siguiendo el mismo procedimiento que exige la Regla 6 pero sin poder ejecutar código. Esa verificación manual encontró un error real en la fuente (ver aviso siguiente). Si en una sesión futura la ejecución de Python vuelve a estar disponible, convendría re-verificar con código los ejemplos de `muestreo-intervalos.md` y `contraste-hipotesis.md` (los de `correlacion.md` ya se corrigieron).
2026-09-24 · L04 · FUENTE · En `correlacion.md`, el material original (03-probabilidad-estadistica/05-est-inferencial.md, sección "Correlación y dependencia") calcula $r$ para $X=[2,4,5,6,8]$ horas de estudio e $Y=[5,6,7,8,9]$ calificaciones y afirma "obtendríamos un valor cercano a 0.97". Verificado a mano: $\bar X=5$, $\bar Y=7$, $\text{cov}(X,Y)=14/5=2{,}8$, $\sigma_X=2$, $\sigma_Y=\sqrt2\approx1{,}414$, por lo que $r=2{,}8/(2\cdot1{,}414)\approx0{,}99$, no $0{,}97$. Corregido con `:::nota-fuente` en la ficha, usando el valor correcto (0,99) en el resto del texto y en el ejemplo de "En código".
2026-09-24 · L04 · DECISIÓN · Redactadas las 3 fichas del lote L04 (bloque B1): `muestreo-intervalos` (fusión de dos fragmentos de la fuente), `contraste-hipotesis`, `correlacion`. `build.py --lote L04 --ejecutar` dio inicialmente 2 errores en `correlacion.md`: la palabra española suelta "todo" en "sobre todo" (reformulada a "en especial", mismo patrón que L01-L03) y un error nuevo, "Bloques ::: desemparejados", en un bloque `:::nota-fuente` bien formado (abre y cierra correctamente, contenido verificado con varias comprobaciones manuales de conteo de líneas). Aislado el problema: el bloque `:::nota-fuente` estaba colocado en mitad de la sección Formalización, con texto normal antes y después dentro de la misma sección; moverlo al final de la sección (mismo patrón que el único bloque `:::ampliacion` que ya existía en el proyecto, en `kmeans.md`, justo antes del siguiente `## `) resolvió el error sin cambiar el contenido de forma sustancial. No se ha identificado la causa exacta en `tools/ficha.py`/`tools/build.py` (la cuenta de líneas `^:::\s*$` vs `^:::\s*\w` parecía balanceada 1-1 en ambas ubicaciones al comprobarlo con búsquedas de texto), así que se deja como PROPUESTA revisar el comprobador de bloques `:::` si se repite en futuros lotes; de momento, la recomendación práctica es colocar siempre los bloques `:::ampliacion`/`:::nota-fuente` al final de su sección. Tras corregir: `build.py --lote L04 --ejecutar`: 0 errores, 3 avisos. `probar_web.py --lote L04`: 3/3 sin errores de consola ni interactivos vacíos.
2026-09-24 · L04 · AVISO · `build.py` marca 3 términos de glosario definidos en más de una ficha, todos por solape entre una ficha nueva de este lote y una ficha ya existente de un bloque distinto: "error estándar" (`dist-normal`, `muestreo-intervalos`), "bootstrap" (`integral-monte-carlo`, `muestreo-intervalos`) y "coeficiente de correlación de Pearson" (`graficos-estadisticos`, `correlacion`). No se han tocado las fichas ajenas al lote (regla de economía de tokens de CLAUDE.md §7 impide abrirlas para comparar cuál lo "introduce" realmente). PROPUESTA: en una revisión (`Revisión Bx`) decidir en cuál ficha de cada par debe quedar la entrada de glosario y dejar en la otra solo el enlace `[[id]]`.

2026-09-24 · L03 · AVISO · SIN PUSH: mismo bloqueo que en sesiones anteriores (ver avisos de L01, L02 y Motores M3-M9 más abajo, aún sin resolver): el commit de L03 (`b697f44`) se hizo bien en local, pero `git push origin main` fue rechazado (`! [rejected] main -> main (fetch first)`, origin/main tiene commits que este repo local no tiene) y `git fetch origin` fue denegado automáticamente por falta de superficie de aprobación en esta sesión autónoma. No se ha intentado ningún merge/rebase ni force-push. Pendiente: ejecutar manualmente `git fetch && git log main..origin/main` para ver qué hay en el remoto, integrarlo y solo entonces hacer `git push`. A estas alturas hay ya varios commits locales sin publicar acumulados de sesiones previas (L01, L02, Motores M1-M9, Ajustes web 1-3); conviene revisarlos todos juntos al resolver este bloqueo.

2026-09-24 · L03 · DECISIÓN · Redactadas las 8 fichas nuevas del lote L03 (bloque B1): `prob-condicional`, `bayes`, `variable-aleatoria`, `dist-normal`, `dist-binomial`, `dist-poisson`, `estadistica-descriptiva`, `graficos-estadisticos`. `build.py --lote L03 --ejecutar` dio inicialmente 10 errores en 6 fichas: la misma palabra española suelta "todo" en `prob-condicional.md` (reformulada), formato de glosario con aclaración entre paréntesis antes de los dos puntos en `dist-binomial.md`, `dist-poisson.md`, `variable-aleatoria.md` y `graficos-estadisticos.md` (movida dentro de la definición, mismo patrón que L02), dos preguntas de autoevaluación con solo 2 opciones en vez de ≥3 (`prob-condicional.md`, `estadistica-descriptiva.md`, añadida una tercera opción plausible), el valor `unidad: visitas/día` del widget `datos1d` sin comillas (JSON inválido) en `estadistica-descriptiva.md` y `graficos-estadisticos.md`, y "En una frase" de `graficos-estadisticos.md` con 36 palabras (recortada a 33). Tras corregir: `build.py --lote L03 --ejecutar`: 0 errores, 0 avisos.
2026-09-24 · L03 · DECISIÓN · `probar_web.py --lote L03` encontró dos problemas reales no achacables al contenido de las fichas. (1) Las expresiones `comb(n,k)*p^k*(1-p)^(n-k)` (dist-binomial) y `lambda^k*exp(-lambda)/fact(k)` (dist-poisson) usaban `k` como variable libre, pero el motor `funcion` siempre evalúa con `x` como variable del eje (`nombres = ['x'].concat(parametros)`, ver `docs/motores/funcion.js`); corregido sustituyendo `k` por `x` en ambos `expr` (el deslizador del modo `discreta` ya se etiqueta "valor k" internamente, así que la ficha no pierde precisión). (2) El modo `tabla` del motor `probabilidad` (implementado en Motores M2, antes de que se detectara la exigencia de `probar_web.py` de que todo `.widget` contenga al menos un `svg`/`canvas`, ver aviso de Motores M5 sobre `matriz-calor`) solo renderizaba una `<table>` HTML y no tenía ningún elemento gráfico; como es un motor ya implementado y no una ficha nueva, se corrigió `docs/motores/probabilidad.js` (función `tabla`) añadiendo una segunda `div.motor-grafica` con un gráfico de barras SVG (`api.svg`/`api.el`, mismo patrón que `venn`/`naive-bayes` en el mismo archivo) que muestra las probabilidades condicionadas o conjuntas según el botón activo, coherente con el texto ya generado en `lectura`. Probado con `probar_web.py --demo probabilidad --movil` (0 errores) y `probar_web.py --lote L03 --movil` (8/8 sin errores de consola ni interactivos vacíos, escritorio y móvil). `build.py --ejecutar` sin `--lote` (las 25 fichas del proyecto): 0 errores, 0 avisos.

Una línea por entrada, la más reciente arriba. Formato: `AAAA-MM-DD · Lxx · tipo · texto`.
Tipos: DECISIÓN (ya aplicada), PROPUESTA (la decide el usuario), AVISO (aviso del build que se deja a propósito), FUENTE (error corregido en la fuente).

2026-09-24 · L02 · AVISO · SIN PUSH: mismo bloqueo que en sesiones anteriores (ver avisos de L01 y de Motores M3-M9 más abajo, aún sin resolver): el commit de L02 se hizo bien en local, pero `git push origin main` fue rechazado (`! [rejected] main -> main (fetch first)`, origin/main tiene commits que este repo local no tiene) y `git fetch origin` fue denegado automáticamente por falta de superficie de aprobación en esta sesión autónoma. No se ha intentado ningún merge/rebase ni force-push. Pendiente: ejecutar manualmente `git fetch && git log main..origin/main` para ver qué hay en el remoto, integrarlo y solo entonces hacer `git push`.
2026-09-24 · L02 · DECISIÓN · Redactadas las 7 fichas nuevas del lote L02 (bloque B1): `autovalores-svd`, `funciones`, `no-linealidad`, `regla-cadena`, `gradiente`, `integral-monte-carlo`, `probabilidad`. Se omitió `derivada` porque el extractor la marcó como ya existente. `build.py --lote L02 --ejecutar` dio inicialmente 6 errores en 4 fichas: el comprobador de TODO/XXX/FIXME/lorem (ya apuntado como PROPUESTA sin resolver, ver aviso de L01 más abajo) volvió a marcar la palabra española suelta "todo" en `funciones.md`, `integral-monte-carlo.md`, `no-linealidad.md` y `probabilidad.md` (reformuladas sin tocar `tools/`), y el formato de glosario rechazó dos entradas con una aclaración entre paréntesis antes de los dos puntos (`**ReLU** (*Rectified Linear Unit*): ...` y `**Espacio muestral** ($\Omega$): ...`), movidas dentro de la propia definición. Tras corregir: `build.py --lote L02 --ejecutar` y `probar_web.py --lote L02`: 0 errores, 0 avisos, 8/8 capturas (incluida `derivada`, ya existente) sin problemas de consola ni interactivos vacíos. Para los parámetros de los widgets de `funcion/familias`, `probabilidad/venn` y `pasos` (regla-cadena) no había ejemplo exacto de esas claves en la salida del extractor más allá de la lista de parámetros aceptados; se completaron con valores razonables siguiendo el patrón de otros modos ya usados en el proyecto, y `probar_web.py` no reportó errores de consola ni widgets vacíos con ellos.
2026-09-24 · L01 · AVISO · Sesión autónoma nueva para "Lote L01": las 7 fichas ya existían sin commit (sesión anterior interrumpida antes del paso 6). Verificado `python tools/build.py --lote L01 --ejecutar`: 10 fichas (incluye las de bloques previos revisadas de paso), 0 errores, 0 avisos. Hecho el commit local (`git add` + `git commit`, sin bloqueo esta vez). `git push origin main` rechazado: `! [rejected] main -> main (fetch first)`, origin/main tiene commits que este repo local no tiene. `git fetch origin` fue denegado automáticamente por falta de superficie de aprobación en esta sesión autónoma (mismo bloqueo descrito en los avisos de Motores M3-M9 más abajo). No se ha intentado ningún merge/rebase ni force-push para no arriesgar el historial sin poder ver antes qué hay en el remoto. Pendiente: ejecutar manualmente `git fetch && git log main..origin/main` para ver los commits remotos (probablemente los de Motores M1-M9, que llevan varias sesiones sin poder publicarse), integrarlos (`git pull --rebase` o `merge`) y solo entonces `git push`.
2026-09-24 · L01 · DECISIÓN · Redactadas las 7 fichas del lote L01 (bloque B1): `vectores`, `producto-escalar-similitud`, `espacio-vectorial`, `matrices`, `datos-como-matrices`, `determinante-inversa`, `sistemas-lineales`. Se eliminó del borrador de `producto-escalar-similitud` un bloque `:::ampliacion` sobre la maldición de la dimensionalidad porque `build.py` avisó de que no estaba previsto en el inventario; no aportaba lo bastante como para justificarlo fuera de lo ya cubierto en Errores típicos, así que se quitó en vez de justificarlo. `build.py --lote L01 --ejecutar` y `probar_web.py --lote L01`: 0 errores, 0 avisos, 7/7 capturas sin problemas de consola ni interactivos vacíos.
2026-09-24 · L01 · AVISO · El comprobador "Contiene TODO/XXX/FIXME/lorem" de `tools/ficha.py` (ya apuntado como PROPUESTA en Fase 1B, sin resolver) marcó como error la palabra española suelta "todo" en varias fichas nuevas (p. ej. "casi todo se convierte", "genera todo el espacio"). No es un TODO real; se reformularon esas frases para no usar la palabra "todo" en solitario (se dejaron "todos", "método", etc., que no disparan el aviso) en vez de tocar `tools/`.
2026-09-24 · L01 · AVISO · SIN PUSH ni commit confirmados todavía: en esta sesión autónoma, `git add`/`git commit`/`git push` pueden requerir la misma aprobación que ya bloqueó sesiones anteriores (ver avisos de Motores M3-M9 más abajo, con hasta 17 commits locales sin publicar). Si este commit tampoco se pudo hacer, ejecuta tú `git add fichas/vectores.md fichas/producto-escalar-similitud.md fichas/espacio-vectorial.md fichas/matrices.md fichas/datos-como-matrices.md fichas/determinante-inversa.md fichas/sistemas-lineales.md DECISIONES.md && git commit -m "Lote L01: vectores, producto-escalar-similitud, espacio-vectorial, matrices, datos-como-matrices, determinante-inversa, sistemas-lineales"` y `git push` si hay remoto (revisando antes `git fetch && git log main..origin/main`, porque las sesiones de Motores dejaron sin publicar varios commits locales).
2026-09-24 · Motores M9 · AVISO · SIN PUSH: mismo bloqueo que en M3-M8 (ver avisos más abajo, aún sin resolver): `git fetch`/`git push` denegados en modo autónomo por falta de superficie de aprobación. El commit de M9 se suma en local a los anteriores sin publicar. Antes de tocar nada, ejecuta `git fetch && git log main..origin/main` para ver qué hay en el remoto.
2026-09-24 · Motores M9 · DECISIÓN · Implementados los 10 modos finales de `dispersion2d`: `knn`, `svm`, `arbol`, `bosque`, `boosting`, `comparar-clustering`, `jerarquico`, `dbscan`, `gmm` y `silueta`, cada uno en `motores/dispersion2d-<modo>.js` (mismo contrato que M8). Añadidas a `dispersion2d.js` tres ayudas compartidas expuestas en `window.Dispersion2d`: `grilla(ctx, g, filas, columnas, evaluar)` (rejilla de celdas coloreadas por clase, aproxima regiones de decisión; usada por knn, bosque y boosting) y `construirArbol/evaluarArbol/regionesArbol` (árbol de decisión por cortes de eje con impureza de Gini, admite pesos por muestra; usado por arbol, bosque y boosting como tocón ponderado). `knn`: punto de consulta arrastrable con líneas a sus k vecinos y círculo de radio = distancia al k-ésimo; el fondo colorea la rejilla con el voto. `svm`: SVM lineal de margen blando por descenso de subgradiente sobre la pérdida bisagra (mismo patrón que `logistica` de M8: dar un primer paso ya en `iniciar`), con control C = 1/λ y vectores de soporte marcados. `arbol`: regiones exactas (no aproximadas) coloreadas a partir de `regionesArbol`, recortando la caja `ctx.dom` en cada corte; sin paso a paso, se recalcula entero en cada `fondo()` porque es barato. `bosque`: bagging de árboles de profundidad fija 3 sobre muestras bootstrap, región de decisión por voto mayoritario vía `grilla`. `boosting`: AdaBoost con tocones (profundidad 1) ponderados; el peso de cada punto se visualiza como un halo (radio ∝ peso) dibujado en `fondo` antes de los puntos, y el ejemplo usa el generador `xor` para mostrar cómo varios tocones lineales se combinan en una frontera no lineal. `comparar-clustering`: mismo dataset agrupado a demanda por k-means, jerárquico (enlace promedio) o DBSCAN (implementaciones propias y ligeras, sin compartir código con los modos dedicados) con silueta media como métrica de comparación; ejemplo con generador `lunas` para que se note la diferencia. `jerarquico`: dendrograma completo calculado una vez (`construirDendrograma`, enlace promedio) y un puntero `revelados` que "Paso →" avanza una fusión cada vez; el panel dibuja la distancia de cada fusión (equivalente en lista al dendrograma, mismo estilo que el codo de kmeans) con la línea de corte en k objetivo. `dbscan`: implementación estándar (núcleo/borde/ruido) con ε y minPts en vivo; el fondo dibuja el halo ε alrededor de cada punto núcleo. `gmm`: EM con paso = un ciclo E+M completo, elipses de 1 desviación típica por componente. `silueta`: agrupa con k-means y dibuja el gráfico de silueta clásico (barras por punto, ordenadas por clúster) en el panel, reutilizando `ctx.celda` (Voronoi) ya expuesto por el núcleo. Probado con `probar_web.py --demo --movil` (los 10 modos nuevos más `dispersion2d` base): 0 errores de consola; capturas revisadas en escritorio y móvil.
2026-09-24 · Motores M9 · DECISIÓN · Corregido un bug real detectado al revisar las capturas de `gmm`: la función que calculaba los autovectores de la covarianza (copiada del patrón de `pca.js` de M8) devolvía el mismo autovector para `l1` y `l2` cuando `sxy ≈ 0` (caso isótropo, muy común al iniciar EM), así que las elipses se dibujaban como una línea plana en vez de un círculo/elipse. Corregido calculando `v2` siempre como perpendicular a `v1` (`[-v1[1], v1[0]]`) en vez de invocar la función dos veces. `pca.js` (M8, ya con commit) tiene la misma función original con el mismo defecto latente, pero ahí solo se usa para dibujar dos ejes (líneas), no una elipse, así que el efecto visual pasó desapercibido en su revisión; lo apunto aquí sin tocar M8 por quedar fuera del alcance de esta sesión. También se cambió la covarianza inicial de gmm para que sea isótropa a propósito (antes usaba la varianza global de cada eje por separado, casi siempre distinta entre sí, lo que ya generaba elipses muy alargadas incluso sin el bug).
2026-09-24 · Motores M8 · AVISO · SIN PUSH: mismo bloqueo que en M3-M7 (ver avisos más abajo, aún sin resolver): `git fetch`/`git push` denegados en modo autónomo por falta de superficie de aprobación. El commit de M8 se suma en local a los anteriores sin publicar. Antes de tocar nada, ejecuta `git fetch && git log main..origin/main` para ver qué hay en el remoto.
2026-09-24 · Motores M8 · PROPUESTA · Al implementar los nuevos modos de `dispersion2d` se detectó un riesgo de referencia obsoleta en el núcleo del motor (`montarModo`, `docs/motores/dispersion2d.js`): `def.botones(ctx)` se invoca **antes** que `def.iniciar(ctx)`, así que si un modo hace `botones(ctx){ const E = ctx.estado; ...}` y luego `iniciar(ctx){ ctx.estado = {...}; }` (reasignando el objeto en vez de mutarlo), la `E` capturada en el cierre de los botones queda apuntando al objeto viejo, desconectada de `ctx.estado` real. Esto es exactamente lo que hace hoy `MODOS.kmeans` (`iniciar` reasigna `ctx.estado = {metodo:'aleatoria', semilla:1}` después de que `botones` ya capturó `E = ctx.estado`): el botón "Inicio: k-means++" cambia el `metodo` del objeto obsoleto, no el que lee `kmReiniciar`, así que puede que no cambie realmente el método de inicialización al pulsarlo (el resaltado del botón sí es coherente consigo mismo, por eso no se notó en las capturas). No lo he tocado por quedar fuera del alcance de M8 (kmeans es de la Fase 1B); en los 8 modos nuevos de M8 se ha evitado el problema mutando siempre `ctx.estado` en el sitio (nunca reasignándolo). Propongo revisar `kmeans` y, si se confirma, cambiar su `iniciar` a `Object.assign(ctx.estado, {...})`.
2026-09-24 · Motores M8 · DECISIÓN · Implementados en `dispersion2d` los 8 modos de regresión/preprocesado: `correlacion`, `ols`, `polinomio`, `ridge-lasso`, `metricas-regresion`, `escalado`, `pca` y `logistica`, cada uno en `motores/dispersion2d-<modo>.js` (contrato ya usado por `kmeans`/`rejilla`/`texto`), con guarda `if (window.Dispersion2d) registrar(); else Motores.cargar('dispersion2d').then(registrar);` para poder cargarse sueltos (lo exige `probar_web.py --demo`, que trata cada `.js` de `docs/motores/` como motor de primer nivel). Añadidas a `dispersion2d.js` unas ayudas numéricas compartidas (`Dispersion2d.mediaDe`, `.regresionSimple`, `.estadisticos`, `.resolver` — Gauss con pivoteo parcial) para no duplicar mínimos cuadrados/estadísticos en cada modo. `polinomio` ajusta por ecuaciones normales sobre una matriz de Vandermonde (grado ajustable, con aviso de sobreajuste si el grado se acerca al número de puntos). `ridge-lasso` usa grado fijo 5 sobre variables estandarizadas: ridge en forma cerrada (con λI, sin regularizar el intercepto porque se centra `y`) y lasso por descenso de coordenadas con umbral suave; panel de barras comparando coeficientes sin regularizar vs. regularizados. `escalado` compara estandarización, mín-máx y robusto (mediana/RIC) con botones, mostrando en el panel una réplica en miniatura de la nube ya escalada. `pca` calcula la descomposición propia 2×2 en forma cerrada (sin iterar) y permite proyectar los puntos sobre la 1ª componente. `logistica` entrena por descenso de gradiente por lotes (paso a paso), dando ya un primer paso en `iniciar`/al cambiar controles o datos para que el estado inicial muestre una frontera en vez de partir de pesos a cero. Todos los modos usan `dataset.clases: 1` cuando el generador por defecto asignaría 2 clases (irrelevante para regresión). Probado con `probar_web.py --demo --movil` (los 9 modos de `dispersion2d`, más `dispersion2d-<modo>` sueltos): 0 errores de consola; capturas revisadas en escritorio y móvil.

2026-09-24 · Motores M7 · AVISO · SIN PUSH: mismo bloqueo que en M3-M6 (ver avisos más abajo, aún sin resolver): `git fetch` fue denegado automáticamente ("sin superficie de aprobación" en modo autónomo). El commit de M7 se suma en local a los anteriores sin publicar. Antes de tocar nada, ejecuta `git fetch && git log main..origin/main` para ver qué hay en el remoto.
2026-09-24 · Motores M7 · DECISIÓN · Implementados `serie` (ventana, drift) y `texto` (limpieza, tokenizacion, bow-tfidf, ngramas) en docs/motores/. Al empezar la sesión ya existían `docs/motores/serie.js` (completo, los dos modos, pasaba `probar_web.py --demo`) y un cambio suelto en `docs/estilos.css` (clase `.motor-textarea`) sin commit, restos de una sesión anterior interrumpida; se han dado por buenos y completado el trabajo que faltaba (`texto` no existía). `texto`: infraestructura común (tokenización por palabras con regex `[a-zA-ZÀ-ÿ0-9]+`, lista de 90 palabras vacías en español, un stemmer de sufijos "de juguete" — no es un algoritmo lingüístico real, solo para ilustrar la idea —, escapado HTML de lo que teclea el usuario antes de inyectarlo con `innerHTML`) en `window.Texto`; `limpieza` y `tokenizacion` viven en `texto.js`, y `bow-tfidf`/`ngramas` se cargan bajo demanda de `texto-<modo>.js` (mismo contrato que `rejilla`/`dispersion2d`, incluido el mismo `ejemplos` con las 4 claves en el `Motores.registrar` base para que la página de demo `#texto` monte también bow-tfidf y ngramas). `limpieza` deja elegir con botones qué pasos de `pasos` (minúsculas, puntuación, números, espacios, palabras vacías, stemming) están activos y muestra la tabla del texto tras cada paso activo, en orden fijo. `tokenizacion` compara partir por espacios (arrastra la puntuación) contra un tokenizador por palabras. `bow-tfidf` calcula idf(t) = ln(N/df(t)) con conteos crudos (no tf normalizada) sobre un corpus editable de varios documentos, con selector de documento, métrica (conteo/TF-IDF) y palabras vacías on/off. `ngramas` desliza una ventana de tamaño n (1-4) sobre los tokens con "Paso →", y tabla + barras de frecuencias. Probado con `probar_web.py --demo --movil` para los dos motores y sus modos en archivo aparte (`texto`, `texto-bow-tfidf`, `texto-ngramas`, `serie`): 0 errores de consola; capturas revisadas en escritorio y móvil.
2026-09-24 · Motores M6 · AVISO · SIN PUSH: el commit del arreglo de la demo se hizo bien en local, pero `git push` fue rechazado (origin/main tiene commits que no están aquí) y `git fetch` requiere aprobación de red que no está disponible en modo autónomo. Antes de nada, mira qué hay en origin/main y, si son commits válidos de otra sesión, intégralos (`git pull --rebase` o `merge`) y haz `git push`.
2026-09-24 · Motores M6 · DECISIÓN · Corregido `probar_web.py --demo`: fallaba en rejilla-iteracion-valores, rejilla-monte-carlo, rejilla-td0 y rejilla-sarsa-qlearning ("Rejilla is not defined", interactivo vacío) porque ese script recorre todos los `.js` de `docs/motores/` como si cada uno fuera un motor de primer nivel, y esos cuatro archivos son modos que `rejilla.js` carga bajo demanda (asumen que `window.Rejilla` ya existe). Arreglado sin tocar `tools/`: cada `rejilla-<modo>.js` ahora espera a `Motores.cargar('rejilla')` si `window.Rejilla` no existe todavía, y `Motores.cargar` (docs/motores/nucleo.js) añade un resguardo genérico: si `motores/<motor>-<modo>.js` no se registra a sí mismo, reintenta como "`<modo>` del motor `<motor>`" reutilizando su ejemplo. `python tools/probar_web.py --demo`: 20/20 motores sin errores.

2026-09-24 · Motores M6 · AVISO · SIN PUSH: mismo bloqueo que en M3-M5 (ver avisos más abajo, aún sin resolver): en modo autónomo no hay superficie para aprobar `git fetch`/`git push` a un origin que ya tenía commits nuevos la primera vez que se detectó (Motores M3). El commit de M6 se suma en local a los 12 anteriores, así que quedan 13 commits locales sin publicar. Antes de tocar nada, ejecuta `git fetch && git log main..origin/main` para ver qué hay en el remoto.
2026-09-24 · Motores M6 · PROPUESTA · Al probar `rejilla` con clics reales (no solo capturas) se detectó que en `grafo.js` y `matriz-calor.js` los `<text>`/adornos que se dibujan encima de cada `rect`/`circle` clicable son hermanos en el SVG (no descendientes), así que un clic justo sobre el texto o icono no llega al `onclick` del `rect` (los navegadores dan los eventos de puntero al elemento pintado más arriba, y ahí no hay burbujeo entre hermanos). No lo he tocado porque no es parte de este lote, pero conviene añadir `pointer-events: none` a esos textos/iconos en una futura revisión, como se hizo aquí en `rejilla.js`.
2026-09-24 · Motores M6 · DECISIÓN · Implementado `rejilla` (los 7 modos: busqueda, mdp, valores, iteracion-valores, monte-carlo, td0, sarsa-qlearning) en docs/motores/. Infraestructura común en `rejilla.js` (expuesta como `window.Rejilla`): parseo del mapa (`.` libre, `#` muro, `S` inicio, `G` meta, `X` trampa, `C` acantilado), transiciones con resbalón (`estocastico`: con esa probabilidad la acción va a uno de los dos perpendiculares, mitad y mitad — enumeradas exactas para planificación, muestreadas con `r()` para aprendizaje), recompensas (G=+1, X=−1, C=−100 siempre; el resto usa `recompensa_paso`, terminal V=0), iteración de valores sincrónica y un dibujo de cuadrícula SVG compartido (`Rejilla.dibujarMundo`, celdas accesibles con flechas de política, valores numéricos, icono de agente y traza). Los modos `busqueda` y `mdp` son exploradores conceptuales (BFS con "Oleada →"/"Hasta el final"; y una tabla de transición/recompensa por acción al seleccionar un estado); `valores` corre la iteración de valores hasta converger y deja elegir γ. Los 4 modos que aprenden de episodios (`iteracion-valores`, `monte-carlo`, `td0`, `sarsa-qlearning`) están cada uno en `rejilla-<modo>.js`, cargado bajo demanda igual que el contrato ya usado por `dispersion2d` (ESPEC-WEB.md §API real), para que ningún archivo pase de ~600 líneas; sus modos se listan igualmente en la línea `// @modos:` de `rejilla.js`, como exige `tools/build.py`. `sarsa-qlearning` usa como ejemplo un "paseo por el acantilado" clásico (fila inferior de `C` entre `S` y `G`) para que se note la diferencia on-policy/off-policy: SARSA aprende una ruta alejada del borde y Q-learning la óptima pegada a él. Probado con `probar_web.py --demo --movil` (0 errores de consola) y, además, con un script Playwright ad-hoc (`capturas/_test_rejilla.py`, borrado tras usarlo por no ser parte de las herramientas del proyecto) que pulsó todos los botones, deslizadores y celdas clicables de los 7 modos; ese script encontró el problema de `pointer-events` descrito arriba (ya corregido en `rejilla.js`).
2026-09-24 · Motores M5 · AVISO · SIN PUSH: mismo bloqueo que en Motores M3 y M4 (ver avisos más abajo, aún sin resolver): `git push` fue rechazado (`! [rejected] main -> main (fetch first)`, origin tiene commits que faltan aquí) y `git fetch` fue denegado automáticamente por falta de superficie de aprobación en modo autónomo. El commit de M5 se sumó en local a los de M1-M4, así que ahora hay 11 commits locales sin publicar. Antes de tocar nada, ejecuta `git fetch && git log main..origin/main` para ver qué hay en el remoto.
2026-09-24 · Motores M5 · DECISIÓN · Implementados `grafo` (diagrama, conocimiento, red-bayesiana), `linea-tiempo` y `matriz-calor` (atencion, mascaras, busqueda, tabla-enlaces) en docs/motores/. `grafo`: diagrama y red-bayesiana ordenan los nodos en capas por profundidad topológica (distancia máxima desde una raíz siguiendo aristas a→b, con orden por baricentro respecto a la capa anterior) y dibujan cajas rectangulares; conocimiento usa un diseño de fuerzas propio (repulsión + muelles, sin depender de mapa.js) calculado una vez con semilla fija y normalizado a [0,1], luego escalado al tamaño del contenedor en cada redibujado. El tamaño del SVG de diagrama/red-bayesiana se calcula a partir del ancho real de las cajas (no de un ancho de contenedor fijo) para que en móvil el SVG completo se achique por CSS en vez de recortarse; se detectó y corrigió justo ese recorte durante las pruebas con `--movil`. red-bayesiana hace inferencia exacta por enumeración (enumeration-ask) sobre variables binarias; formato de `tabla` no está en widgets.json (solo dice "+ tabla: {...}"): cada fila es `{dado: {idPadre: 0|1, ...}, p: P(nodo=1 | dado)}`, con una sola fila `{dado:{}, p}` si el nodo no tiene padres; clic en un nodo cicla sin observar → 1 → 0 → sin observar. `matriz-calor` dibuja la rejilla en SVG (no en `<table>` HTML) para cumplir ESPEC-WEB.md §8 ("SVG o Canvas") y porque `tools/probar_web.py` exige al menos un `svg`/`canvas` dentro de `.widget`; cada celda es un `<rect>` con `tabindex`/`role=button` accesible por teclado. Probado con `probar_web.py --demo --movil`: 0 errores de consola; capturas revisadas en escritorio y móvil (incluida la corrección del recorte en móvil).
2026-09-24 · Motores M4 · AVISO · SIN PUSH: mismo bloqueo que en Motores M3 (ver aviso más abajo): `git push` rechazado porque origin/main tiene commits que faltan aquí, y en modo autónomo no hay superficie para aprobar `git fetch`. El commit de M4 se sumó en local a los de M1-M3, así que ahora hay 9 commits locales sin publicar. Antes de tocar nada, revisa `git fetch && git log main..origin/main`.
2026-09-24 · Motores M4 · DECISIÓN · Implementados `red` (perceptron, xor, forward, backprop) y `convolucion` en docs/motores/. `red` comparte un dibujo de red común: aristas coloreadas por signo (acento/mal) y grosor/opacidad por |valor| normalizado por transición; esa misma función visualiza pesos o, en `backprop`, gradientes (∂L/∂W) una vez calculados. Formato de `pesos`: lista por transición `{W, b}` con `W` de forma (neuronas_siguiente × neuronas_anterior); si la ficha no da `pesos`, se generan de forma reproducible con `api.aleatorio(7)`. `backprop` hace retropropagación analítica (no por diferencias finitas) con pérdida ½Σ(a−y)², paso a paso: primero revela la propagación hacia delante capa a capa, luego las deltas hacia atrás, y termina con un botón "Aplicar un paso de descenso" (η = 0,3, fijo, no expuesto como parámetro) que actualiza los pesos y recalcula. `convolucion` desliza el kernel en orden fila por fila con `Paso →`/`Hasta el final`, colorea las tres rejillas (entrada, salida, agrupado) con escala fija precalculada (para que el color no cambie al ir revelando celdas) y usa relleno con ceros y pooling 2×2 fijo (no configurable) cuando `pooling` no es "ninguno". Probado con `probar_web.py --demo --movil` para los dos motores: 0 errores de consola; capturas revisadas en escritorio y móvil.
2026-09-24 · Motores M3 · AVISO · SIN PUSH: el commit se hizo bien en local, pero `git push` fue rechazado porque origin/main tiene commits que no están aquí, y en modo autónomo no se pudo ejecutar `git fetch` (requiere aprobación de red, sin superficie para concederla). Antes de hacer nada, mira qué hay en origin/main (`git fetch && git log main..origin/main`): si son commits de otra sesión de trabajo válida, intégralos (`git pull --rebase` o `merge`) y haz `git push`; si es un estado inesperado, revísalo con cuidado antes de sobrescribir nada.
2026-09-24 · Motores M3 · DECISIÓN · Implementados `simulacion` (pi, intervalos, bandido, policy-gradient) y `descenso` (gradiente, lr, optimizadores) en docs/motores/. `descenso` dibuja la superficie f(x, y) como mapa de calor + curvas de nivel (marching squares simplificado, sin resolver el caso de silla de forma especial) sobre dos `<canvas>` apilados: uno estático (fondo, caro, solo se recalcula al redimensionar) y uno dinámico (frente: punto, flecha o trayectorias, barato, se redibuja en cada arrastre o paso) para que arrastrar el punto en el modo `gradiente` no recalcule la rejilla en cada `pointermove`. Usa ejes a la misma escala (como `dispersion2d`) para que las curvas de nivel de una cuadrática no salgan deformadas. `simulacion/intervalos` incluye una aproximación propia de la inversa de la normal (algoritmo de Acklam) porque `api.expr` no expone `erf`/`erfinv` fuera del motor de expresiones. Probado con `probar_web.py --demo --movil` para los dos motores: 0 errores de consola; capturas revisadas en escritorio y móvil.
2026-09-24 · Motores M2 · DECISIÓN · Implementados `probabilidad` (venn, tabla, test, naive-bayes, softmax) y `datos1d` (centralidad, graficos, outliers, imputacion) en docs/motores/. venn resuelve la distancia entre centros por búsqueda binaria para que el área de la intersección sea proporcional a P(A∩B) (no solo aproximada). test usa Canvas (no SVG) porque dibuja 1000 iconos, por encima del límite de ~500 elementos de ESPEC-WEB.md §8. datos1d comparte un ayudante `carriles()` (beeswarm simple) para no solapar puntos casi coincidentes, y `imputacion` admite `null` dentro de `valores` para representar huecos. Probado con `probar_web.py --demo --movil` para los dos motores: 0 errores de consola; capturas revisadas en escritorio y móvil (se corrigió un solape de texto en el medidor de naive-bayes que solo aparecía en anchos estrechos).
2026-09-24 · Motores M1 · DECISIÓN · Implementados `vectores2d` (operaciones, similitud, base, etiquetas) y `transformacion2d` (rejilla, determinante, autovectores) en docs/motores/. Vectores y columnas de la matriz son arrastrables (arrastre + flechas del teclado) sobre un dominio fijo (no se recalcula al mover, para que los ejes no salten); operaciones y similitud comparten el mismo dibujo con una lista `mostrar` seleccionable. Probado con `probar_web.py --demo --movil`: 0 errores de consola; capturas revisadas en escritorio y móvil.
2026-09-24 · Ajustes web 3 · AVISO · SIN COMMIT: en modo autónomo se denegó `git add`/`git commit`. Ejecuta tú `git add -A && git commit -m "Ajustes web 3: filtro por bloques, selección de nodos y panel de detalle en el mapa"` (y `git push` si hay remoto).
2026-09-24 · Ajustes web 3 · DECISIÓN · Aplicado AJUSTES-WEB-3.md en docs/app.js, docs/mapa.js y docs/estilos.css: chips de filtro por bloque (`filtroBloques` en el progreso), clic = seleccionar (ya no abre la ficha), resaltado de requisitos (naranja, por distancia), desbloquea (verde) y conexiones/desambiguaciones (azul discontinuo) en las dos vistas, y panel de detalle (lateral en escritorio, hoja inferior en móvil). Extra conservador: ir desde el panel o el buscador a un concepto de un bloque filtrado añade su bloque al filtro. En móvil, "Por bloques" solo marca los nodos (sin curvas). Descrito en ESPEC-WEB.md §4 y §6. Probado con `probar_web.py --vistas --movil` y capturas de `kmeans` y `producto-escalar-similitud` seleccionados en las dos vistas y en móvil.
2026-09-24 · Ajustes web 2 · DECISIÓN · Aplicado AJUSTES-WEB-2.md: "## En resumen" en derivada, metricas-clasificacion y kmeans. En docs/app.js la sección "En resumen" es un único nodo que `ponModo` mueve: en Esencial va justo tras la frase con el título "En resumen"; en Completa va tras Errores típicos como recuadro "Para llevar". Esencial oculta Intuición, Explicación entera (ya no deja títulos `###` sueltos), Formalización, En código, A fondo, Glosario y Fuentes, y ahora muestra el Interactivo.
2026-09-24 · Ajustes web 2 · AVISO · Solo se revisaron capturas de la vista Completa: `probar_web.py` no captura la vista Esencial y en modo autónomo se denegó ejecutar `capturas/esencial.py`, el script auxiliar que la captura. Conviene ejecutarlo a mano (`python capturas/esencial.py`) o mirar Esencial en el navegador.
2026-09-24 · Ajustes web 1 · DECISIÓN · Aplicado AJUSTES-WEB-1.md: `#mapa` abre la "Constelación" (docs/mapa.js, grafo de fuerzas determinista) con "Por bloques" como alternativa; `#rutas` dibuja cada ruta como red por capas con "Ver en la constelación" (`ultimaRuta` en el progreso); el glosario incluye los 138 conceptos, barra A-Z, filtro por bloque y contador. Descrito en ESPEC-WEB.md §2, §4 y §6.
2026-09-24 · Ajustes web 1 · AVISO · Solo se pudieron revisar capturas en tema claro: `probar_web.py` no captura el tema oscuro y en modo autónomo no se permiten scripts fuera de `tools/`. Conviene mirar la constelación en oscuro a mano.
2026-09-24 · Fase 1B · DECISIÓN · dispersion2d carga los modos que no estén en su archivo desde `motores/dispersion2d-<modo>.js` (contrato en ESPEC-WEB.md §API real), para que M8/M9 no superen ~600 líneas por archivo.
2026-09-24 · Fase 1B · DECISIÓN · Corregido el arrastre en `funcion` (y aplicado en umbral/dispersion2d): la captura del puntero se hacía sobre el SVG, que se sustituye en cada redibujado, así que el arrastre se cortaba tras el primer movimiento.
2026-09-24 · Fase 1B · PROPUESTA · `tools/ficha.py` (PROHIBIDO, con re.I) trata la palabra española "todo" como TODO: obliga a evitar "sobre todo", "lo es todo"… Propongo quitar `re.I` o exigir mayúsculas para TODO/XXX/FIXME.
2026-09-24 · Fase 1B · DECISIÓN · En la ficha kmeans el parámetro `controles` lleva `etiqueta` (no listada en widgets.json, que solo da `tipo: lista`); el motor la usa como texto del deslizador.
2026-09-24 · Fase 1B · AVISO · SIN COMMIT si git sigue rechazando el repositorio por propiedad dudosa (ver entrada de la Fase 1A): tras el `git config --global --add safe.directory`, haz `git add -A && git commit -m "Fase 1A y 1B"`.
2026-09-24 · Fase 1A · AVISO · SIN COMMIT: git rechaza el repositorio por propiedad dudosa (la carpeta es de BUILTIN/Administradores y el usuario es luck9), y el modo autónomo no puede usar `git config --global --add safe.directory C:/Users/luck9/atlas-ia` ni `git -c safe.directory=…`. Ejecuta tú esa orden `git config` y después `git add -A && git commit -m "Fase 1A: web base, motores pasos y funcion, ficha derivada"`.
2026-09-24 · Fase 1A · PROPUESTA · build.py convierte `fotogramas` a `[{html}]` y descarta `tabla`/`cajas`/`activa` si vienen en JSON. El motor `pasos` los admite (se ven en demo.html), pero en las fichas solo sirven los fotogramas Markdown con `---`. Si quieres cajas o celdas resaltadas en las fichas, hay que conservar esas claves en `tools/build.py`.
2026-09-24 · Fase 1A · DECISIÓN · `api.expr` admite más funciones de las que lista widgets.json (tanh, fact, comb, erf, floor, sign…) para poder escribir la binomial, la normal acumulada, Huber o las pertenencias difusas. Están documentadas en ESPEC-WEB.md §API real.
2026-09-24 · Fase 1A · DECISIÓN · El tema elegido a mano se guarda en `localStorage['atlas-ia-tema']` y la vista Completa/Esencial en `atlas-ia-modo`, fuera de `atlas-ia-v1`, para que al exportar el progreso solo salga el progreso.
2026-09-23 · — · DECISIÓN · Inventario v1 (138 conceptos, 31 lotes) aprobado por el usuario. Reparto exclusivo del texto fuente: cada línea pertenece a la reclamación más específica.
